# RealEstate CRM System

Комплексная CRM система для управления недвижимостью с поддержкой жилых комплексов, планировок квартир, лидов и финансового учета.

## 🚀 Возможности

- **Управление ЖК** - Каталог жилых комплексов с интерактивными планировками
- **CRM для продаж** - Управление лидами, клиентами и сделками
- **Финансовый учет** - Платежи, расходы, доходы
- **Маркетинг** - Кампании и реклама
- **Операции с недвижимостью** - MOP/ROP функционал
- **Telegram бот** - Автоматизация уведомлений
- **Публичные лендинги** - Маркетинговые страницы ЖК

## 🛠 Технологии

### Backend
- **FastAPI** - Современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Alembic** - Миграции базы данных
- **JWT** - Аутентификация
- **Celery** - Асинхронные задачи
- **Google Sheets API** - Интеграция с Google Sheets
- **Aiogram** - Telegram бот

### Frontend
- **HTML5/CSS3/JavaScript** - Веб-интерфейс
- **Jinja2** - Шаблонизатор
- **Responsive Design** - Адаптивный дизайн

## 📦 Установка

### Локальная разработка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd RealEstate
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

5. **Запустите миграции**
```bash
alembic upgrade head
```

6. **Запустите приложение**
```bash
uvicorn main:app --reload
```

### Docker

1. **Запустите с Docker Compose**
```bash
docker-compose up -d
```

2. **Приложение будет доступно по адресу**
- http://localhost:8000 - Основное приложение
- http://localhost:8000/api/docs - API документация

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Database
DATABASE_URL=sqlite:///data.db

# JWT Settings
SECRET_KEY=your_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Telegram Bot
BOT_TOKEN=your_bot_token_here

# Instagram Integration
INSTAGRAM_APP_ID=your_instagram_app_id
INSTAGRAM_APP_SECRET=your_instagram_app_secret
INSTAGRAM_REDIRECT_URI=http://localhost:8000/api/instagram/callback

# Google Sheets
GOOGLE_SHEETS_API_KEY=your_google_api_key
GOOGLE_CREDENTIALS_PATH=path/to/credentials.json

# Email Settings
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Environment
ENVIRONMENT=development
DEBUG=True
```

## 📚 API Документация

После запуска приложения API документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/api/docs
- **ReDoc**: http://localhost:8000/api/redoc

## 📱 Интеграция Instagram
- Настройте переменные `INSTAGRAM_APP_ID`, `INSTAGRAM_APP_SECRET`, `INSTAGRAM_REDIRECT_URI` и зарегистрируйте redirect URL в приложении Instagram.
- В админке откройте `/dashboard/admin/marketing/instagram/connect`, нажмите «Подключить Instagram» и завершите авторизацию.
- После подключения используйте дашборд `/dashboard/admin/marketing/instagram` для просмотра профиля и медиа.

## 🏗 Архитектура

```
RealEstate/
├── backend/           # Backend приложение
│   ├── api/          # API эндпоинты
│   ├── crm/          # CRM модули
│   ├── core/         # Основные модули
│   ├── database/     # База данных
│   └── bot/          # Telegram бот
├── frontend/         # Frontend приложение
├── static/           # Статические файлы
└── alembic/          # Миграции БД
```

## 🔐 Безопасность

- JWT аутентификация
- Валидация входных данных
- Rate limiting
- Заголовки безопасности
- Логирование всех операций

## 📊 Мониторинг

- Структурированное логирование
- Метрики производительности
- Обработка ошибок
- Health checks

## 🤝 Разработка

### Запуск в режиме разработки

```bash
# Запуск с hot reload
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Запуск Celery worker
celery -A celery_proccess worker --loglevel=info

# Запуск Celery beat
celery -A celery_proccess beat --loglevel=info
```

### Создание миграций

```bash
# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
alembic upgrade head
```

## 📝 Лицензия

Этот проект является частной собственностью.

## 🆘 Поддержка

Для получения поддержки обращайтесь к команде разработки.
