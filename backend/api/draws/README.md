# 🎁 Draws API — Управление розыгрышами

## Описание

Модуль для управления розыгрышами (конкурсами) для привлечения клиентов через Telegram бота.

## Назначение

Розыгрыши используются для:
- Привлечения новых клиентов
- Сбора контактных данных (телефон)
- Увеличения узнаваемости бренда
- Вовлечения аудитории в соцсетях

## Процесс розыгрыша

```
┌─────────────────┐
│  Telegram бот   │  Пользователь регистрируется через бота
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    DrawUser     │  Данные сохраняются в БД
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Excel экспорт  │  Администратор экспортирует участников
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Розыгрыш      │  Случайный выбор победителя
└─────────────────┘
```

## Эндпоинты

### Экспорт участников

#### `GET /draw-users/export`
Экспортировать список зарегистрированных пользователей в Excel.

**Query параметры:**
- `skip` — сколько записей пропустить (по умолчанию 0)
- `limit` — максимум записей (1-1000, по умолчанию 200)

**Response:** Excel файл (.xlsx)

**Структура файла:**

| ID | Telegram ID | Имя | Фамилия | Телефон | Дата регистрации |
|----|-------------|-----|---------|---------|------------------|
| 1  | 123456789   | Иван| Петров  | +998... | 2025-12-01 10:00 |
| 2  | 987654321   | Мария| Сидорова| +998... | 2025-12-01 11:30 |

**Пример запроса:**
```
GET /draw-users/export?skip=0&limit=500
```

**Использование:**
```javascript
// Скачать Excel файл
function downloadParticipants() {
  window.location.href = '/draw-users/export?limit=1000';
}
```

## Модели БД

### DrawUser
```python
- id: int (PK)
- telegram_id: int (UNIQUE)    # ID пользователя в Telegram
- first_name: str              # Имя
- last_name: str (optional)    # Фамилия
- phone: str                   # Телефон (+998XXXXXXXXX)
- created_at: datetime         # Дата регистрации
```

## CRUD функции

Расположение: `backend/database/marketing/crud.py`

### DrawUserCRUD

```python
class DrawUserCRUD:
    def create_draw_user(db, telegram_id, first_name, phone, last_name=None):
        """Создать участника розыгрыша"""
        
    def get_draw_user_by_telegram(db, telegram_id):
        """Найти участника по Telegram ID"""
        
    def list_draw_users(db, skip=0, limit=100):
        """Получить список участников"""
        
    def get_draw_users_count(db):
        """Получить общее количество участников"""
```

## Интеграция с Telegram ботом

### Регистрация через бота

```python
# В Telegram боте
from backend.database.marketing.crud import DrawUserCRUD

@bot.message_handler(commands=['participate'])
def participate_in_draw(message):
    user = message.from_user
    phone = ask_for_phone(user)  # Запрос телефона
    
    # Сохранение в БД
    DrawUserCRUD().create_draw_user(
        db=db,
        telegram_id=user.id,
        first_name=user.first_name,
        last_name=user.last_name,
        phone=phone
    )
    
    bot.reply_to(message, "Вы зарегистрированы в розыгрыше!")
```

### Команды бота

- `/participate` — участвовать в розыгрыше
- `/mystatus` — проверить статус участия
- `/winners` — список победителей (после розыгрыша)

## Проведение розыгрыша

### Экспорт участников

1. Администратор открывает админ панель
2. Переходит в "Маркетинг" → "Розыгрыши"
3. Нажимает "Экспортировать участников"
4. Скачивается Excel файл со всеми участниками

### Выбор победителя

**Вариант 1: Вручную в Excel**
```
1. Открыть файл draw_users.xlsx
2. Использовать формулу =СЛУЧМЕЖД(2, СЧЁТЗ(A:A))
3. Выбрать победителя по случайному номеру строки
```

**Вариант 2: Автоматически (планируется)**
```python
# TODO: Добавить эндпоинт для автоматического выбора
@router.post("/draw-users/select-winner")
def select_random_winner(db: Session = Depends(get_db)):
    users = DrawUserCRUD().list_draw_users(db, limit=10000)
    winner = random.choice(users)
    # Сохранить победителя в таблицу Winners
    return {"winner": winner}
```

## Примеры использования

### JavaScript: Экспорт в Excel

```javascript
async function exportParticipants() {
  const response = await fetch('/draw-users/export?limit=1000');
  const blob = await response.blob();
  
  // Создать ссылку для скачивания
  const url = window.URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'participants.xlsx';
  a.click();
  
  window.URL.revokeObjectURL(url);
}
```

### JavaScript: Статистика участников

```javascript
async function getParticipantsCount() {
  // Пока нет эндпоинта для count, используем экспорт
  const response = await fetch('/draw-users/export?limit=1');
  // Парсинг заголовков для получения общего количества
  
  // TODO: Добавить эндпоинт GET /draw-users/count
}
```

## Права доступа

Роль | Экспорт | Просмотр | Проведение розыгрыша
-----|---------|----------|----------------------
Админ | ✅ | ✅ | ✅
РОП | ✅ | ✅ | ✅
МОП | ✅ | ✅ | ❌
Маркетинг | ✅ | ✅ | ❌
Продажник | ❌ | ❌ | ❌

## Аналитика

### Метрики для отслеживания

- **Количество участников** — общее число зарегистрированных
- **Конверсия в лиды** — сколько участников стали лидами
- **Динамика регистраций** — по дням/часам
- **Источники трафика** — откуда пришли участники

### Пример отчёта

```
Розыгрыш "Квартира в ЖК Рассвет"
Период: 01.12.2025 - 15.12.2025

Участников: 1,250
Новых лидов: 87 (6.96%)
Закрытых сделок: 3 (0.24%)

Топ дни регистрации:
1. 01.12.2025 — 456 регистраций (старт розыгрыша)
2. 14.12.2025 — 234 регистрации (напоминание)
3. 15.12.2025 — 189 регистраций (последний день)
```

## Юридические аспекты

### Согласие на обработку данных

При регистрации пользователь должен согласиться:
- На обработку персональных данных
- На получение рекламных сообщений
- С правилами розыгрыша

### Хранение данных

- Данные хранятся в БД с шифрованием
- Телефоны используются только для связи с победителями
- Пользователи могут запросить удаление своих данных (GDPR)

## TODO

- [ ] Добавить эндпоинт для подсчёта участников
- [ ] Автоматический выбор победителя
- [ ] Таблица Winners для хранения победителей
- [ ] История розыгрышей
- [ ] Уведомления победителям через Telegram
- [ ] Статистика по источникам регистрации
- [ ] Интеграция с Instagram для розыгрышей в Stories

---

**Автор:** RealEstate CRM Team  
**Дата:** 2025

