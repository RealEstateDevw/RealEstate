# 🎯 Leads API — Управление лидами (сделками)

## Описание

Модуль для управления лидами (потенциальными клиентами). Включает создание, обновление, назначение продажникам, комментарии, историю изменений и уведомления.

## Файлы

- `main.py` — FastAPI эндпоинты
- `schemas.py` — Pydantic схемы
- `schedule_notification.py` — Уведомления в Telegram

## Основные концепции

### Статусы лида

Статус | Описание | Следующий шаг
-------|----------|---------------
`новый` | Только что создан | Назначить продажнику
`в работе` | Продажник работает с клиентом | Перевести в "закрыт" или "отказ"
`закрыт` | Сделка успешно закрыта | —
`отказ` | Клиент отказался | —

### Источники лидов

- Instagram
- Facebook
- Google Ads
- Рекомендация
- Холодный звонок
- Сайт

### Жизненный цикл лида

```
┌─────────────┐
│   Создание  │  Лид создаётся МОП/РОП
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Новый     │  Статус: "новый"
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Назначение │  Лид назначается продажнику
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  В работе   │  Продажник работает с клиентом
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Закрытие  │  Статус: "закрыт" или "отказ"
└─────────────┘
```

## Эндпоинты

### Создание лида

#### `POST /api/leads/create`
Создать новый лид.

**Request:**
```json
{
  "client_name": "Иван Петров",
  "phone_number": "+998901234567",
  "source": "Instagram",
  "budget": 50000,
  "notes": "Интересуется 2-комнатной квартирой",
  "assigned_to": 5
}
```

**Response:**
```json
{
  "id": 123,
  "client_name": "Иван Петров",
  "phone_number": "+998901234567",
  "status": "новый",
  "source": "Instagram",
  "created_at": "2025-12-03T10:00:00Z",
  "assigned_to": {
    "id": 5,
    "first_name": "Продажник",
    "last_name": "Иванов"
  }
}
```

### Получение лидов

#### `GET /api/leads/`
Получить список лидов с фильтрацией.

**Query параметры:**
- `status` — фильтр по статусу ("новый", "в работе", и т.д.)
- `source` — фильтр по источнику
- `assigned_to` — фильтр по продажнику
- `page` — номер страницы (по умолчанию 1)
- `size` — размер страницы (по умолчанию 20)

**Response:**
```json
{
  "items": [
    {
      "id": 123,
      "client_name": "Иван Петров",
      "phone_number": "+998901234567",
      "status": "в работе",
      "source": "Instagram",
      "created_at": "2025-12-03T10:00:00Z"
    },
    ...
  ],
  "total": 45,
  "page": 1,
  "size": 20,
  "pages": 3
}
```

#### `GET /api/leads/{lead_id}`
Получить детальную информацию о лиде.

**Response:**
```json
{
  "id": 123,
  "client_name": "Иван Петров",
  "phone_number": "+998901234567",
  "status": "в работе",
  "source": "Instagram",
  "budget": 50000,
  "notes": "Интересуется 2-комнатной квартирой",
  "created_at": "2025-12-03T10:00:00Z",
  "updated_at": "2025-12-03T14:30:00Z",
  "assigned_to": {
    "id": 5,
    "first_name": "Продажник",
    "last_name": "Иванов"
  },
  "comments": [
    {
      "id": 1,
      "text": "Позвонил клиенту, договорились на встречу",
      "created_by": "Продажник Иванов",
      "created_at": "2025-12-03T11:00:00Z"
    }
  ],
  "history": [
    {
      "action": "status_changed",
      "old_value": "новый",
      "new_value": "в работе",
      "changed_by": "Продажник Иванов",
      "changed_at": "2025-12-03T10:30:00Z"
    }
  ]
}
```

### Обновление лида

#### `PATCH /api/leads/{lead_id}`
Обновить данные лида.

**Request:**
```json
{
  "status": "закрыт",
  "notes": "Клиент купил квартиру №45",
  "closing_date": "2025-12-05"
}
```

#### `POST /api/leads/{lead_id}/assign`
Назначить лид другому продажнику.

**Request:**
```json
{
  "assigned_to": 7
}
```

### Комментарии

#### `POST /api/leads/{lead_id}/comment`
Добавить комментарий к лиду.

**Request:**
```json
{
  "text": "Клиент попросил отправить планировки"
}
```

**Response:**
```json
{
  "id": 15,
  "text": "Клиент попросил отправить планировки",
  "created_by": {
    "id": 5,
    "first_name": "Продажник",
    "last_name": "Иванов"
  },
  "created_at": "2025-12-03T15:00:00Z"
}
```

### Удаление

#### `DELETE /api/leads/{lead_id}`
Удалить лид (только для админов и РОП).

## Модели БД

### Lead
```python
- id: int (PK)
- client_name: str
- phone_number: str
- status: str (новый, в работе, закрыт, отказ)
- source: str
- budget: float (optional)
- notes: str (optional)
- assigned_to: int (FK -> User, optional)
- created_by: int (FK -> User)
- created_at: datetime
- updated_at: datetime
- closing_date: date (optional)
```

### Comment
```python
- id: int (PK)
- lead_id: int (FK -> Lead)
- text: str
- created_by: int (FK -> User)
- created_at: datetime
```

### LeadHistory
```python
- id: int (PK)
- lead_id: int (FK -> Lead)
- action: str (status_changed, assigned, updated)
- old_value: str (optional)
- new_value: str (optional)
- changed_by: int (FK -> User)
- changed_at: datetime
```

## CRUD функции

Расположение: `backend/database/sales_service/crud.py`

- `create_lead(db, data)` — создать лид
- `get_lead_by_id(db, lead_id)` — получить лид по ID
- `get_leads(db, filters)` — получить список лидов с фильтрами
- `update_lead(db, lead_id, data)` — обновить лид
- `delete_lead(db, lead_id)` — удалить лид
- `add_comment(db, lead_id, text, user_id)` — добавить комментарий
- `add_history(db, lead_id, action, old, new, user_id)` — добавить запись в историю

## Уведомления

Модуль `schedule_notification.py` отправляет уведомления в Telegram:

### Когда отправляются уведомления

1. **Новый лид** — уведомление назначенному продажнику
2. **Смена статуса** — уведомление МОП/РОП
3. **Переназначение** — уведомление новому продажнику
4. **Напоминание** — если лид долго в статусе "новый"

### Пример уведомления

```
🎯 Новый лид!

Клиент: Иван Петров
Телефон: +998901234567
Источник: Instagram
Бюджет: $50,000

Назначен: Продажник Иванов
```

## Примеры использования

### Создание лида через JavaScript

```javascript
async function createLead(data) {
  const response = await fetch('/api/leads/create', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
      client_name: data.name,
      phone_number: data.phone,
      source: data.source,
      budget: data.budget,
      notes: data.notes,
      assigned_to: data.sellerId
    })
  });
  
  if (!response.ok) {
    throw new Error('Ошибка создания лида');
  }
  
  return response.json();
}

// Использование
createLead({
  name: "Иван Петров",
  phone: "+998901234567",
  source: "Instagram",
  budget: 50000,
  notes: "Звонил по объявлению",
  sellerId: 5
}).then(lead => {
  console.log('Создан лид:', lead.id);
  alert('Лид успешно создан!');
});
```

### Обновление статуса лида

```javascript
async function updateLeadStatus(leadId, newStatus) {
  const response = await fetch(`/api/leads/${leadId}`, {
    method: 'PATCH',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({status: newStatus})
  });
  
  return response.json();
}

// Закрыть лид
updateLeadStatus(123, 'закрыт');
```

### Добавление комментария

```javascript
async function addComment(leadId, text) {
  const response = await fetch(`/api/leads/${leadId}/comment`, {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({text})
  });
  
  return response.json();
}

// Использование
addComment(123, 'Клиент попросил перезвонить завтра');
```

## Права доступа

Роль | Создание | Просмотр | Обновление | Удаление
-----|----------|----------|------------|----------
Админ | ✅ | Все | Все | ✅
РОП | ✅ | Все | Все | ✅
МОП | ✅ | Все | Все | ❌
Продажник | ❌ | Только свои | Только свои | ❌
Финансист | ❌ | Все | ❌ | ❌

## Интеграции

### Instagram
Лиды из Instagram автоматически создаются через webhook или ручной импорт.

### Telegram Bot
Уведомления о новых лидах и изменениях отправляются в Telegram.

### Google Sheets
Экспорт лидов в Google таблицы для аналитики.

## Разработка

### Добавление нового источника

1. Добавить в список источников в `schemas.py`:
```python
SOURCE_CHOICES = [
    "Instagram",
    "Facebook",
    "Google Ads",
    "Новый источник"  # <-- добавить
]
```

2. Обновить frontend для отображения нового источника.

### Добавление нового статуса

1. Добавить в enum `LeadStatus` в `models.py`
2. Обновить логику в `main.py`
3. Добавить обработку в frontend

## Ошибки

Код | Описание | Причина
----|----------|--------
400 | Bad Request | Невалидные данные (например, неправильный телефон)
401 | Unauthorized | Нет токена
403 | Forbidden | Попытка изменить чужой лид (для продажников)
404 | Not Found | Лид не найден
409 | Conflict | Лид с таким телефоном уже существует

## TODO

- [ ] Автоматическое определение дубликатов по телефону
- [ ] Экспорт лидов в Excel/PDF
- [ ] Интеграция с CRM системами (AmoCRM, Bitrix24)
- [ ] Напоминания о просроченных задачах
- [ ] Аналитика конверсии по источникам

---

**Автор:** RealEstate CRM Team  
**Дата:** 2025









